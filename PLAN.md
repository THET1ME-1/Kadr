# 🎬 Kadr — план разработки

**Kadr (Кадр)** — трекер просмотренных фильмов и сериалов. Material 3 Expressive,
крупный необычный дизайн, статистика и кастомизация на максимуме.
ДНК инфраструктуры — из **ScoreMaster** (тема, i18n, настройки, синк).

> Легенда: `[x]` сделано · `[~]` частично · `[ ]` в планах

---

## 🎨 Дизайн-направление (утверждено)

- **Material 3 Expressive** — крупные формы, выразительная типографика, пружинные
  анимации, «shared-axis» переходы между экранами (уже в теме).
- **Шрифты**: Unbounded (заголовки/крупные цифры) + Onest (текст), кириллица + латиница.
- **Кнопки** — закрашенные (`FilledButton`), широкие, со скруглением «таблетка»
  (`StadiumBorder`, высота 56). Уже зашито в теме.
- **Все попапы — снизу** (`showModalBottomSheet`, скругление сверху 28, ручка).
  Никаких центральных диалогов.
- **Иконки** — бесплатные M3 (`Icons.*_rounded/_outlined`), без платных наборов.
- **Оценки — настроения** (emoji-mood, как в референсе: Ужасно → Восхитительно),
  а не только сухие звёзды. Цветовая шкала красный→зелёный.
- **Кастомизация**: тёмная/светлая/системная/авто-по-времени, Material You (цвет
  из обоев), AMOLED, живой колор-пикер (HSV-колесо + HEX), 8 палитр.
- **Крупные постеры**, сетка/список переключаемо (кнопка «Стиль» в AppBar).
- **Группировка просмотров по месяцам** («Просмотры октябрь 2023 г.»), как в референсе.

### Экраны (по референсу + улучшения)
1. **Оболочка** — 4 вкладки снизу: Буду смотреть / Просмотрено / Обзор / В кино
   + выезжающее меню (Главная, Поиск, Статистика, Списки, Настройки, О приложении).
2. **Карточка фильма** — герой-бэкдроп, постер+название+год+хронометраж+слоган,
   рейтинг + твоя оценка-настроение, жанры; ряд действий (Просмотрено / Списки /
   Буду смотреть / Другое / Избранное); описание, режиссёр, бюджет/сборы, тест
   Бекдел, внешние ссылки (Кинопоиск/IMDb/TMDb/Rotten Tomatoes/Trakt/Wikipedia).
   Вкладки ИНФО / ОТЗЫВЫ (гистограмма рейтинга 1–10).
3. **«Когда посмотрели?»** — нижняя панель (Неизвестная дата / Только что завершил /
   Сегодня в… / Вчера в… / Выберите дату). Несколько просмотров на фильм.
4. **Поиск** — по TMDB, живой, с постерами.
5. **Статистика** — крупная, богатая (см. раздел ниже).
6. **Списки** — свои списки с иконкой/цветом/счётчиком (Избранное, Мультфильмы, 18+, …).
7. **Настройки** — внешний вид, язык (7), данные/синк, о приложении.
8. **Фильтры/Сортировка/Стиль** — по году, жанру, актёрам, просмотрено-или-нет, +свои.

---

## 🎞 База фильмов — решение: **TMDB (основной)**

**The Movie Database (TMDB)** — оптимальный бесплатный источник:
- ✅ **Бесплатный API** (ключ по регистрации), некоммерческое использование.
- ✅ **Русский И английский из коробки**: `language=ru-RU` / `en-US` отдаёт
  локализованные названия, описания И **русские постеры** (именно поэтому в
  референсе плашки «RUS»). Плюс `region` для дат проката.
- ✅ Богатые данные: постеры/бэкдропы, рейтинг+число голосов, хронометраж, жанры,
  актёры/съёмочная группа, бюджет/сборы, слоган, IMDb-id, коллекции.
- ✅ **Сериалы**: сезоны, эпизоды, даты выхода.
- ✅ Trending / Popular / Now Playing (в кино) / Discover (фильтры по жанру/году/актёру)
  — точно закрывает вкладки и фильтры референса.
- ✅ CDN картинок `image.tmdb.org`, щедрые лимиты (~50 rps).

**Дополнительные источники (по мере надобности, опционально):**
- **OMDb API** (бесплатно 1000/день) — рейтинги IMDb / Rotten Tomatoes / Metacritic
  (для блоков рейтингов и внешних ссылок как в референсе).
- **Trakt** — распределение оценок 1–10 (гистограмма отзывов), скроблинг. Нужен OAuth — поздний этап.
- **Kinopoisk (kinopoisk.dev)** — сообществный русский API, бесплатный тариф —
  если захотим показывать оценку Кинопоиска для РФ-аудитории.

**Ключ API**: репозиторий приватный → допустимо хранить ключ, но правильнее —
`--dart-define=TMDB_KEY=…` при сборке (решим на этапе интеграции).

---

## 💾 Данные, синхронизация и бэкап (как в ScoreMaster)

- **Локально-первичное** хранилище: слой `MovieRepository` (`ChangeNotifier`) поверх
  `Store` (SharedPreferences сейчас; при росте объёма — миграция на Hive/Isar).
  Кэш метаданных фильмов из TMDB + пользовательские просмотры/оценки/списки/заметки.
- **Бэкап JSON**: экспорт/импорт всех данных (`file_picker` + `share_plus`) —
  ровно как в ScoreMaster (не OAuth, чтобы не хранить секреты).
- **Синхронизация между устройствами**: переносится готовый слой ScoreMaster —
  `services/sync/` (WebDAV, P2P, папка-бэкап, `sync_merge`). Подключим после MVP.
- В бэкап входят: списки, просмотры (даты+оценки), заметки, оценки-настроения,
  избранное, И настройки (тема/цвет/язык/стиль).

---

## 🧱 Что уже перенесено из ScoreMaster (готово)

- [x] Проект Flutter `Kadr` в `~/Projects/GitHub/Kadr` (org `com`, applicationId `com.kadr`, label «Kadr»)
- [x] **Тема** `theme/app_theme.dart` — M3 Expressive (Unbounded/Onest, pill-кнопки, shared-axis, волнистые индикаторы)
- [x] **Менеджер темы** `theme/theme_controller.dart` — seed-цвет + 4 режима + Material You + AMOLED, live-применение
- [x] **Менеджер языков** `l10n/locale_controller.dart` — **7 языков** (ru/en/de/fr/es/it/pt), автоопределение по системе
- [x] **Строки** `l10n/strings.dart` (ru/en) + `translations.dart` (базовый каркас de/fr/es/it/pt)
- [x] **Живой колор-пикер** `widgets/color_picker_sheet.dart` (HSV-колесо + HEX)
- [x] **Слой хранения** `services/store.dart`
- [x] **Форматтеры** `utils/format.dart`, анимация `widgets/reveal.dart`
- [x] **Экран настроек** `screens/settings_screen.dart` — внешний вид / язык / данные / о приложении (всё нижними панелями)
- [x] **Оболочка** `screens/home_shell.dart` — 4 вкладки + меню, крупный expressive стиль
- [x] `flutter analyze` — 0 замечаний

---

## 🗺 Дорожная карта

### Этап 0 — Фундамент ✅
- [x] Скаффолд, тема, i18n, настройки, оболочка, колор-пикер, `analyze=0`
- [x] Приватный репозиторий на GitHub
- [ ] Сборка APK и запуск на телефоне (Redmi Note 9S) — проверка вживую
- [ ] Иконка приложения (кино-мотив, M3-плоская)

### Этап 1 — Интеграция TMDB
- [ ] `services/tmdb_service.dart` — клиент (search, details, trending, now_playing, discover)
- [ ] Модели `Movie` / `TvShow` / `Genre` / `CastMember` (+ `mediaType`)
- [ ] Ключ API через `--dart-define`, обработка ошибок/ретраи/кэш
- [ ] Локализация запросов по текущему языку (`ru-RU`/`en-US`/…)
- [ ] Изображения через `cached_network_image`

### Этап 2 — Ядро трекинга
- [ ] `MovieRepository` (`ChangeNotifier`): просмотры, оценки-настроения, заметки
- [ ] Экран карточки фильма (ИНФО/ОТЗЫВЫ, ряд действий, внешние ссылки)
- [ ] Панель «Когда посмотрели?» + несколько просмотров на фильм
- [ ] Вкладка «Просмотрено» — группировка по месяцам, крупные постер-карточки
- [ ] Вкладка «Буду смотреть» + «В кино» (now playing) + «Обзор» (trending)
- [ ] Поиск фильмов

### Этап 3 — Списки и организация
- [ ] Свои списки с иконкой/цветом/счётчиком (Избранное, Мультфильмы, 18+, Другое…)
- [ ] Управление списками из карточки (нижняя панель)
- [ ] Фильтры (год/жанр/актёры/просмотрено) + сортировка + стиль (сетка/список)

### Этап 4 — Статистика (максимум)
- [ ] Всего просмотрено / часов / по годам-месяцам (календарь-тепловая карта)
- [ ] Любимые жанры/режиссёры/актёры, распределение оценок-настроений
- [ ] Гистограмма рейтингов, «время у экрана», серии просмотров
- [ ] Крупные hero-кольца/бары, count-up анимации (перенос из ScoreMaster)

### Этап 5 — Данные и синхронизация
- [ ] Бэкап/восстановление JSON (`file_picker`/`share_plus`)
- [ ] Перенос слоя `services/sync/` из ScoreMaster (WebDAV/P2P/папка)
- [ ] Настройки данных (создать копию / восстановить / автосинк)

### Этап 6 — Полировка i18n и релиз
- [ ] Полный перевод всех экранов на 7 языков (сейчас каркас)
- [ ] Онбординг, «О приложении», внешние ссылки
- [ ] Релизный keystore, сборка, публикация (Obtainium/GitHub Releases)

---

## 💡 Идеи «сверху» (по желанию)
- Виджет на рабочий стол «сейчас смотрю» / «фильм дня».
- Импорт истории из других трекеров (CSV / Trakt).
- Рекомендации «что посмотреть» на основе оценок.
- Совместные списки для пары/друзей (перекликается с P2P-слоем).
- Быстрые действия (long-press ярлыки): поиск / случайный фильм из «Буду смотреть».

---

*Название `Kadr` можно поменять. Альтернативы: Кинолента, Сеанс, Афиша, Reelog, CineMark.*
